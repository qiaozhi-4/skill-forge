# 注释规则

## 单行注释

- 单行注释统一使用 `///`，不要使用 `//`。
- 普通说明、TODO 和语句行尾注释也使用 `///`。
- 如果任务触及已有的 `//` 行注释，在任务范围允许且转换安全时，将其改为 `///`。

## 块注释与文档注释

- 多行 API 文档或确实需要多行的说明使用 `/** ... */`。
- 只有语法要求或内容确实是块注释而非多条单行注释时，才使用 `/* ... */`。
