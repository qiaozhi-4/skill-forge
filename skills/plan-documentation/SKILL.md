---
name: plan-documentation
description: 规范多步骤项目计划的创建与维护：先生成带状态、优先级、依赖和详情链接的主表，再为每个计划项创建独立详情文档。Use when the user asks to create, structure, update, or review a roadmap, refactoring plan, migration plan, implementation plan, or project checklist. Do not use for simple one-step task lists or when the user only wants a prose explanation.
---

# Plan Documentation

将计划组织成“主路线图 + 事项详情文档”的结构。主路线图是状态总览；详情文档是每个计划项唯一的详细说明来源。

## 核心结构

默认将计划文件放在项目已有的计划目录中。若项目没有约定，优先使用：

```text
.agents/
├── <TOPIC>-ROADMAP.md
└── <topic>/
    ├── 01-<item-slug>.md
    ├── 02-<item-slug>.md
    └── ...
```

如果用户指定了目录、文件名或现有文档，遵循用户和现有项目约定，不要擅自重命名或搬迁已有计划。

## 创建流程

1. 明确计划目标、范围、当前状态和完成标准。
2. 识别计划项之间的依赖关系，给每项分配稳定的顺序编号。
3. 先创建主路线图，并在其中放置计划项总表。
4. 为总表中的每一项创建一个独立详情文档。
5. 将总表的“详情”列链接到对应详情文档，使用从主路线图位置出发的相对路径。
6. 检查每个表格行都有对应详情文档，并检查每个详情文档都被主表引用。
7. 若计划涉及多个阶段，可在主路线图中追加里程碑表和全局约束，但不要把单项的详细方案重复写入主表。

不要只创建一张没有详情链接的表，也不要把多个独立计划项合并到一个详情文档中。

## 主路线图要求

主路线图至少包含：

- 文档用途和维护说明；
- 状态约定；
- 计划项总表；
- 计划项之间的依赖关系；
- 每项的详情链接。

推荐的总表列：

```markdown
| 顺序 | 状态 | 优先级 | 计划项 | 核心目标 | 前置依赖 | 详情 |
|---:|:---:|:---:|---|---|---|---|
| 01 | ⬜ 待开始 | P0 | 示例事项 | 说明要解决的问题 | 无 | [详情](./<topic>/01-example.md) |
```

默认状态可以使用：

- `⬜ 待开始`：尚未实施；
- `🟨 进行中`：已开始，但验收条件尚未全部满足；
- `✅ 已完成`：实现和验收均已完成；
- `⛔ 阻塞`：存在明确阻塞条件；
- `⏸ 暂缓`：主动推迟，不进入当前迭代。

如果项目已有状态体系，保留项目现有定义。优先级和编号应保持稳定，不要因为完成状态变化而重新编号。

## 详情文档要求

每个详情文档只描述一个计划项，并至少包含：

- 标题、编号、状态和优先级；
- 计划项要解决的问题；
- 背景和当前状态；
- 目标与非目标；
- 影响范围；
- 前置依赖和约束；
- 分阶段实施步骤；
- 验收标准；
- 风险、替代方案和阻塞条件；
- 相关文件、测试或验证方式。

具体模板见 [references/plan-item-template.md](references/plan-item-template.md)。只有在创建或修改详情文档时才需要读取该模板。

## 维护规则

- 状态、优先级、依赖或目标发生变化时，同时更新主表和对应详情文档。
- 实施方案发生变化时，先更新详情文档，再更新主表中的摘要。
- 主表只保留可扫描的摘要；详细技术方案、步骤和验收证据放在详情文档中。
- 不要把未确认的内容写成事实；使用“假设”“待确认”或“阻塞”明确标记。
- 计划文档描述要做什么以及如何验收；除非用户明确要求，不要因此直接修改业务代码或执行实施动作。
- 修改已有计划时，保留已有编号、链接和项目约定，避免无关格式重排。

## 创建完成前的检查

确认以下条件全部满足：

- 主路线图能独立说明计划目的和整体进度；
- 总表每行都有唯一编号、状态、优先级、目标、依赖和详情链接；
- 每个详情链接都指向实际存在的文档；
- 每个详情文档只对应一个总表事项；
- 详情文档包含明确的验收标准，而不是只有描述性文字；
- 依赖关系没有形成未说明的循环；
- 状态和进度在主表与详情文档之间一致。
